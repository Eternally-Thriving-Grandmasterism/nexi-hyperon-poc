nexi-hyperon-poc/
├── .gitmodules                  # Auto-generated by submodule adds
├── Cargo.toml                   # Root workspace manifest
├── README.md                    # Setup + usage instructions (copy from below)
├── examples/
│   └── mercy_demo.metta         # Sample MeTTa script testing the gate
├── integration/                 # New crate: NEXi-grounded atoms for Hyperon
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs               # Registers NEXi grounded atoms/space
│       └── nexi_grounded.rs     # Mercy-gate + PQ-sign implementations
├── repl/                        # Custom REPL binary loading the integration
│   ├── Cargo.toml
│   └── src/
│       └── main.rs              # Loads integration space + runs MeTTa REPL
└── deps/                        # Submodules
    ├── hyperon-experimental/    # Full Hyperon repo (workspace with lib, hyperon-atom, etc.)
    └── NEXi/                    # Your NEXi workspace (mercy_quanta, dilithium_sign, etc.)
